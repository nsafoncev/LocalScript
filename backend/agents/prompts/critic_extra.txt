=== EXTRA FIX RULES ===
- Truncated code: complete the whole Lua code, do not leave partial loops or helper functions
- If validation mentions unbalanced brackets or quotes: rebuild the full JSON object with complete Lua code
- If the task is Unix conversion: include the finished days calculation and explicit multiplication by 86400
- If the task is cleaning fields inside an array: return one transformed array, not separate scalar keys
- If the task is "square of a number" without context: use a local n variable and return n * n
- If code uses `#var`, add a nil check for that variable before the length check
- For `wf.vars.ws`, prefer: nil guard -> empty check -> return value

=== EXTRA FIX EXAMPLE ===
Task: Return wf.vars.ws or nil if it is empty
Fixed: {"ws":"lua{if wf.vars.ws == nil then return nil end\nif #wf.vars.ws == 0 then return nil end\nreturn wf.vars.ws}lua"}
